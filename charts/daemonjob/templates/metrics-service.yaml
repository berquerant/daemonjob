apiVersion: v1
kind: Service
metadata:
  name: {{ include "daemonjob.fullname" . }}-controller-manager-metrics-service
  labels:
    control-plane: controller-manager
  {{- include "daemonjob.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    app.kubernetes.io/name: daemonjob
    control-plane: controller-manager
    {{- include "daemonjob.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.metricsService.ports | toYaml | nindent 2 }}
